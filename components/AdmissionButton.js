import React from 'react';

const AdmissionButton = () => {
    return (
        <div className="admission-button">
            <button>Join and Apply for Admission</button>
        </div>
    );
};

export default AdmissionButton;
